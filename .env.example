# Copy this file to .env and fill in your own values.

# --- Your own Postgres instance ---
DATABASE_URL=postgresql+psycopg://forge:forge@localhost:5432/forge

# --- Auth ---
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=change-me-to-a-long-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=30

# --- AI plan generation ---
# Optional shared fallback — individual users can also add their own key via
# PUT /anthropic-key, which is preferred over this one when set.
ANTHROPIC_API_KEY=your-anthropic-api-key
ANTHROPIC_MODEL=claude-sonnet-5

# --- File storage (progress photos) ---
STORAGE_DIR=storage

# --- CORS ---
# Comma-separated list of origins the frontend is served from.
CORS_ORIGINS=http://localhost:8081,http://localhost:19006
